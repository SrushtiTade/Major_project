<nav class="navbar navbar-expand-md bg-body-light border-bottom sticky-top">
  <div class="container-fluid px-4">

    <!-- Brand -->
    <a class="navbar-brand fw-bold" href="/">
      <i class="fa-solid fa-compass me-1" style="color:#fe424d;"></i>WanderLust
    </a>

    <!-- Toggler -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
      data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">
      <!-- Left links -->
      <ul class="navbar-nav me-auto">
        <li class="nav-item">
          <a class="nav-link" href="/listings">Explore</a>
        </li>
        <% if(currUser) { %>
        <li class="nav-item">
          <a class="nav-link" href="/listings/new">+ Add Listing</a>
        </li>
        <% } %>
      </ul>

      <!-- Right links -->
      <ul class="navbar-nav ms-auto">
        <% if(!currUser) { %>
          <li class="nav-item">
            <a class="nav-link" href="/signup">Sign Up</a>
          </li>
          <li class="nav-item">
            <a class="nav-link btn btn-sm add-btn text-white px-3 ms-2" href="/login">Log In</a>
          </li>
        <% } else { %>
          <li class="nav-item">
            <span class="nav-link text-muted">
              <i class="fa-solid fa-circle-user me-1"></i><%= currUser.username %>
            </span>
          </li>
          <li class="nav-item">
            <a class="nav-link btn btn-sm btn-outline-danger px-3 ms-2" href="/logout">Log Out</a>
          </li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>